---
title: WPF 및 Direct3D9 상호 운용성
ms.date: 03/30/2017
dev_langs:
- cpp
helpviewer_keywords:
- WPF [WPF], creating Direct3D9 content
- Direct3D9 [WPF interoperability], creating Direct3D9 content
ms.assetid: 1b14b823-69c4-4e8d-99e4-f6dade58f89a
ms.openlocfilehash: 9fd5cc270074a3a2845147bcad8baef8d1f8ba2a
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 01/23/2019
ms.locfileid: "54529413"
---
# <a name="wpf-and-direct3d9-interoperation"></a><span data-ttu-id="e2d39-102">WPF 및 Direct3D9 상호 운용성</span><span class="sxs-lookup"><span data-stu-id="e2d39-102">WPF and Direct3D9 Interoperation</span></span>
<span data-ttu-id="e2d39-103">Windows Presentation Foundation (WPF) 응용 프로그램에서 호스팅할 Direct3D9 콘텐츠를 포함할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-103">You can include Direct3D9 content in a Windows Presentation Foundation (WPF) application.</span></span> <span data-ttu-id="e2d39-104">이 항목에서는 WPF를 사용 하 여 효율적으로 상호 운용 되도록 Direct3D9 콘텐츠를 만드는 방법을 설명 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-104">This topic describes how to create Direct3D9 content so that it efficiently interoperates with WPF.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="e2d39-105">WPF에서 Direct3D9 콘텐츠를 사용할 때 성능에 대해 고려해 야 하는 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-105">When using Direct3D9 content in WPF, you also need to think about performance.</span></span> <span data-ttu-id="e2d39-106">성능을 최적화 하는 방법에 대 한 자세한 내용은 참조 하세요. [Direct3D9 및 WPF 상호 운용성을 위한 성능 고려 사항](../../../../docs/framework/wpf/advanced/performance-considerations-for-direct3d9-and-wpf-interoperability.md)합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-106">For more information about how to optimize for performance, see [Performance Considerations for Direct3D9 and WPF Interoperability](../../../../docs/framework/wpf/advanced/performance-considerations-for-direct3d9-and-wpf-interoperability.md).</span></span>  
  
## <a name="display-buffers"></a><span data-ttu-id="e2d39-107">버퍼를 표시 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-107">Display Buffers</span></span>  
 <span data-ttu-id="e2d39-108"><xref:System.Windows.Interop.D3DImage> 클래스 라고 하는 두 디스플레이 버퍼를 관리 합니다 *백 버퍼* 하며 *프런트 버퍼가*.</span><span class="sxs-lookup"><span data-stu-id="e2d39-108">The <xref:System.Windows.Interop.D3DImage> class manages two display buffers, which are called the *back buffer* and the *front buffer*.</span></span> <span data-ttu-id="e2d39-109">백 버퍼는 프로그램 Direct3D9 화면입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-109">The back buffer is your Direct3D9 surface.</span></span> <span data-ttu-id="e2d39-110">백 버퍼에 대 한 변경 내용에 복사 됩니다 프런트 버퍼가 호출 하는 경우는 <xref:System.Windows.Interop.D3DImage.Unlock%2A> 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-110">Changes to the back buffer are copied forward to the front buffer when you call the <xref:System.Windows.Interop.D3DImage.Unlock%2A> method.</span></span>  
  
 <span data-ttu-id="e2d39-111">다음 그림에서는 백 버퍼 및 프런트 버퍼가 간의 관계를 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-111">The following illustration shows the relationship between the back buffer and the front buffer.</span></span>  
  
 <span data-ttu-id="e2d39-112">![D3DImage 디스플레이 버퍼](../../../../docs/framework/wpf/advanced/media/d3dimage-buffers.png "D3DImage_buffers")</span><span class="sxs-lookup"><span data-stu-id="e2d39-112">![D3DImage display buffers](../../../../docs/framework/wpf/advanced/media/d3dimage-buffers.png "D3DImage_buffers")</span></span>  
  
## <a name="direct3d9-device-creation"></a><span data-ttu-id="e2d39-113">Direct3D9 장치 만들기</span><span class="sxs-lookup"><span data-stu-id="e2d39-113">Direct3D9 Device Creation</span></span>  
 <span data-ttu-id="e2d39-114">Direct3D9 콘텐츠를 렌더링 하려면 Direct3D9 장치를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-114">To render Direct3D9 content, you must create a Direct3D9 device.</span></span> <span data-ttu-id="e2d39-115">장치를 만드는 데 사용할 수 있는 Direct3D9 개체 두 개가 `IDirect3D9` 고 `IDirect3D9Ex`입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-115">There are two Direct3D9 objects that you can use to create a device, `IDirect3D9` and `IDirect3D9Ex`.</span></span> <span data-ttu-id="e2d39-116">이러한 개체를 만드는 데 `IDirect3DDevice9` 고 `IDirect3DDevice9Ex` 장치, 각각.</span><span class="sxs-lookup"><span data-stu-id="e2d39-116">Use these objects to create `IDirect3DDevice9` and `IDirect3DDevice9Ex` devices, respectively.</span></span>  
  
 <span data-ttu-id="e2d39-117">다음 방법 중 하나를 호출 하 여 장치를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-117">Create a device by calling one of the following methods.</span></span>  
  
-   `IDirect3D9 * Direct3DCreate9(UINT SDKVersion);`  
  
-   `HRESULT Direct3DCreate9Ex(UINT SDKVersion, IDirect3D9Ex **ppD3D);`  
  
 <span data-ttu-id="e2d39-118">Windows Vista 또는 이후 운영 체제에서 사용 된 `Direct3DCreate9Ex` Windows 표시 드라이버 모델 (WDDM)를 사용 하도록 구성 된 표시를 사용 하 여 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-118">On Windows Vista or later operating system, use the `Direct3DCreate9Ex` method with a display that is configured to use the Windows Display Driver Model (WDDM).</span></span> <span data-ttu-id="e2d39-119">사용 된 `Direct3DCreate9` 다른 플랫폼에서 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-119">Use the `Direct3DCreate9` method on any other platform.</span></span>  
  
### <a name="availability-of-the-direct3dcreate9ex-method"></a><span data-ttu-id="e2d39-120">가용성 Direct3DCreate9Ex 메서드</span><span class="sxs-lookup"><span data-stu-id="e2d39-120">Availability of the Direct3DCreate9Ex method</span></span>  
 <span data-ttu-id="e2d39-121">d3d9.dll에는 `Direct3DCreate9Ex` Windows Vista 또는 이후 운영 체제 에서만 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-121">The d3d9.dll has the `Direct3DCreate9Ex` method only on Windows Vista or later operating system.</span></span> <span data-ttu-id="e2d39-122">이 기능을 Windows XP에서 직접 연결 하면 응용 프로그램이 로드 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-122">If you directly link the function on Windows XP, your application fails to load.</span></span> <span data-ttu-id="e2d39-123">결정할 여부는 `Direct3DCreate9Ex` 메서드는 지원 DLL을 로드 하 고 프로시저 주소를 찾습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-123">To determine whether the `Direct3DCreate9Ex` method is supported, load the DLL and look for the proc address.</span></span> <span data-ttu-id="e2d39-124">다음 코드를 테스트 하는 방법을 보여 줍니다는 `Direct3DCreate9Ex` 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-124">The following code shows how to test for the `Direct3DCreate9Ex` method.</span></span> <span data-ttu-id="e2d39-125">전체 코드 예제를 참조 하세요. [연습: WPF에서 호스팅할 Direct3D9 콘텐츠 만들기](../../../../docs/framework/wpf/advanced/walkthrough-creating-direct3d9-content-for-hosting-in-wpf.md)합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-125">For a full code example, see [Walkthrough: Creating Direct3D9 Content for Hosting in WPF](../../../../docs/framework/wpf/advanced/walkthrough-creating-direct3d9-content-for-hosting-in-wpf.md).</span></span>  
  
 [!code-cpp[System.Windows.Interop.D3DImage#RendererManager_EnsureD3DObjects](../../../../samples/snippets/cpp/VS_Snippets_Wpf/System.Windows.Interop.D3DImage/cpp/renderermanager.cpp#renderermanager_ensured3dobjects)]  
  
### <a name="hwnd-creation"></a><span data-ttu-id="e2d39-126">HWND 만들기</span><span class="sxs-lookup"><span data-stu-id="e2d39-126">HWND Creation</span></span>  
 <span data-ttu-id="e2d39-127">장치를 만드는 HWND 필요 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-127">Creating a device requires an HWND.</span></span> <span data-ttu-id="e2d39-128">일반적으로 사용 하는 Direct3D9에 대 한 더미 HWND를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-128">In general, you create a dummy HWND for Direct3D9 to use.</span></span> <span data-ttu-id="e2d39-129">다음 코드 예제에는 더미 HWND를 만드는 방법을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-129">The following code example shows how to create a dummy HWND.</span></span>  
  
 [!code-cpp[System.Windows.Interop.D3DImage#RendererManager_EnsureHWND](../../../../samples/snippets/cpp/VS_Snippets_Wpf/System.Windows.Interop.D3DImage/cpp/renderermanager.cpp#renderermanager_ensurehwnd)]  
  
### <a name="present-parameters"></a><span data-ttu-id="e2d39-130">현재 매개 변수</span><span class="sxs-lookup"><span data-stu-id="e2d39-130">Present Parameters</span></span>  
 <span data-ttu-id="e2d39-131">해야 장치를 만드는 `D3DPRESENT_PARAMETERS` 구조체 있지만 몇 가지 매개 변수만 중요 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-131">Creating a device also requires a `D3DPRESENT_PARAMETERS` struct, but only a few parameters are important.</span></span> <span data-ttu-id="e2d39-132">이러한 매개 변수는 메모리 사용 공간을 최소화 하기 위해 선택 됩니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-132">These parameters are chosen to minimize the memory footprint.</span></span>  
  
 <span data-ttu-id="e2d39-133">설정 된 `BackBufferHeight` 및 `BackBufferWidth` 1 필드입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-133">Set the `BackBufferHeight` and `BackBufferWidth` fields to 1.</span></span> <span data-ttu-id="e2d39-134">0으로 설정 하면 HWND의 크기를 설정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-134">Setting them to 0 causes them to be set to the dimensions of the HWND.</span></span>  
  
 <span data-ttu-id="e2d39-135">항상 설정 합니다 `D3DCREATE_MULTITHREADED` 및 `D3DCREATE_FPU_PRESERVE` 방지 하기 위해 플래그 Direct3D9에서 호스팅할 Direct3D9 FPU 설정을 변경 하지 않도록 설정 하려면 사용 하는 메모리를 손상 시 키 지 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-135">Always set the `D3DCREATE_MULTITHREADED` and `D3DCREATE_FPU_PRESERVE` flags to prevent corrupting memory used by Direct3D9 and to prevent Direct3D9 from changing FPU settings.</span></span>  
  
 <span data-ttu-id="e2d39-136">다음 코드를 초기화 하는 방법을 보여 줍니다는 `D3DPRESENT_PARAMETERS` 구조체입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-136">The following code shows how to initialize the `D3DPRESENT_PARAMETERS` struct.</span></span>  
  
 [!code-cpp[System.Windows.Interop.D3DImage#Renderer_Init](../../../../samples/snippets/cpp/VS_Snippets_Wpf/System.Windows.Interop.D3DImage/cpp/renderer.cpp#renderer_init)]  
  
## <a name="creating-the-back-buffer-render-target"></a><span data-ttu-id="e2d39-137">백 버퍼 렌더링 대상 만들기</span><span class="sxs-lookup"><span data-stu-id="e2d39-137">Creating the Back Buffer Render Target</span></span>  
 <span data-ttu-id="e2d39-138">호스팅할 Direct3D9 콘텐츠를 표시 하는 <xref:System.Windows.Interop.D3DImage>, Direct3D9 화면을 만들고 호출 하 여 할당을 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-138">To display Direct3D9 content in a <xref:System.Windows.Interop.D3DImage>, you create a Direct3D9 surface and assign it by calling the <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> method.</span></span>  
  
### <a name="verifying-adapter-support"></a><span data-ttu-id="e2d39-139">어댑터 지원 확인</span><span class="sxs-lookup"><span data-stu-id="e2d39-139">Verifying Adapter Support</span></span>  
 <span data-ttu-id="e2d39-140">화면을 만들기 전에 모든 어댑터 화면 속성이 필요한 지를 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-140">Before creating a surface, verify that all adapters support the surface properties you require.</span></span> <span data-ttu-id="e2d39-141">어댑터를 하나만 렌더링 하는 경우에 시스템의 WPF 창의 모든 어댑터에서 표시 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-141">Even if you render to only one adapter, the WPF window may be displayed on any adapter in the system.</span></span> <span data-ttu-id="e2d39-142">항상 다중 어댑터 구성을 처리 하는 Direct3D9 코드를 작성 해야 하 고 WPF 간에 사용 가능한 어댑터 화면을 이동할 수 있으므로 지원에 대 한 모든 어댑터를 확인 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-142">You should always write Direct3D9 code that handles multi-adapter configurations, and you should check all adapters for support, because WPF might move the surface among the available adapters.</span></span>  
  
 <span data-ttu-id="e2d39-143">다음 코드 예제에서는 모든 어댑터가 Direct3D9 시스템에서 지 원하는 확인 하는 방법을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-143">The following code example shows how to check all adapters on the system for Direct3D9 support.</span></span>  
  
 [!code-cpp[System.Windows.Interop.D3DImage#RendererManager_TestSurfaceSettings](../../../../samples/snippets/cpp/VS_Snippets_Wpf/System.Windows.Interop.D3DImage/cpp/renderermanager.cpp#renderermanager_testsurfacesettings)]  
  
### <a name="creating-the-surface"></a><span data-ttu-id="e2d39-144">화면 만들기</span><span class="sxs-lookup"><span data-stu-id="e2d39-144">Creating the Surface</span></span>  
 <span data-ttu-id="e2d39-145">화면을 만들기 전에 대상 운영 체제에서 장치 기능 좋은 성능을 지를 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-145">Before creating a surface, verify that the device capabilities support good performance on the target operating system.</span></span> <span data-ttu-id="e2d39-146">자세한 내용은 [Direct3D9 및 WPF 상호 운용성을 위한 성능 고려 사항](../../../../docs/framework/wpf/advanced/performance-considerations-for-direct3d9-and-wpf-interoperability.md)합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-146">For more information, see [Performance Considerations for Direct3D9 and WPF Interoperability](../../../../docs/framework/wpf/advanced/performance-considerations-for-direct3d9-and-wpf-interoperability.md).</span></span>  
  
 <span data-ttu-id="e2d39-147">장치 기능을 확인 하는 경우에 화면을 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-147">When you have verified device capabilities, you can create the surface.</span></span> <span data-ttu-id="e2d39-148">다음 코드 예제에는 렌더링 대상을 만드는 방법을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-148">The following code example shows how to create the render target.</span></span>  
  
 [!code-cpp[System.Windows.Interop.D3DImage#Renderer_CreateSurface](../../../../samples/snippets/cpp/VS_Snippets_Wpf/System.Windows.Interop.D3DImage/cpp/renderer.cpp#renderer_createsurface)]  
  
### <a name="wddm"></a><span data-ttu-id="e2d39-149">WDDM</span><span class="sxs-lookup"><span data-stu-id="e2d39-149">WDDM</span></span>  
 <span data-ttu-id="e2d39-150">Windows Vista 이상 운영 체제를 WDDM을 사용 하도록 구성 된 대상에 렌더링 대상 질감을 만들를 수준 0 화면을 전달 합니다 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-150">On Windows Vista and later operating systems, which are configured to use the WDDM, you can create a render target texture and pass the level 0 surface to the <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> method.</span></span> <span data-ttu-id="e2d39-151">잠글 수 있는 렌더링 대상 질감을 만들 수 없습니다. 성능은 저하 됩니다 때문에 Windows xp에서는이 방법은 권장 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-151">This approach is not recommended on Windows XP, because you cannot create a lockable render target texture and performance will be reduced.</span></span>  
  
## <a name="handling-device-state"></a><span data-ttu-id="e2d39-152">장치 상태 처리</span><span class="sxs-lookup"><span data-stu-id="e2d39-152">Handling Device State</span></span>  
 <span data-ttu-id="e2d39-153"><xref:System.Windows.Interop.D3DImage> 클래스 라고 하는 두 디스플레이 버퍼를 관리 합니다 *백 버퍼* 하며 *프런트 버퍼가*.</span><span class="sxs-lookup"><span data-stu-id="e2d39-153">The <xref:System.Windows.Interop.D3DImage> class manages two display buffers, which are called the *back buffer* and the *front buffer*.</span></span> <span data-ttu-id="e2d39-154">백 버퍼는 프로그램 Direct3D 화면입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-154">The back buffer is your Direct3D surface.</span></span>  <span data-ttu-id="e2d39-155">백 버퍼에 대 한 변경 내용에 복사 됩니다 프런트 버퍼가 호출할 때를 <xref:System.Windows.Interop.D3DImage.Unlock%2A> 메서드를 하드웨어에 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-155">Changes to the back buffer are copied forward to the front buffer when you call the <xref:System.Windows.Interop.D3DImage.Unlock%2A> method, where it is displayed on the hardware.</span></span> <span data-ttu-id="e2d39-156">경우에 따라 프런트 버퍼가 수 없게 됩니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-156">Occasionally, the front buffer becomes unavailable.</span></span> <span data-ttu-id="e2d39-157">화면 잠금, Direct3D 애플리케이션을 제외 하는 전체 화면, 사용자 전환 또는 기타 시스템 활동과이 부족 한 가용성을 발생할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-157">This lack of availability can be caused by screen locking, full-screen exclusive Direct3D applications, user-switching, or other system activities.</span></span> <span data-ttu-id="e2d39-158">WPF 애플리케이션을 처리 하 여 알려집니다 이런 경우는 <xref:System.Windows.Interop.D3DImage.IsFrontBufferAvailableChanged> 이벤트입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-158">When this occurs, your WPF application is notified by handling the <xref:System.Windows.Interop.D3DImage.IsFrontBufferAvailableChanged> event.</span></span>  <span data-ttu-id="e2d39-159">애플리케이션이 사용할 수 없게 되는 프런트 버퍼가에 반응 하는 방법을 소프트웨어 렌더링으로 대체 WPF 사용 여부에 따라 달라 집니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-159">How your application responds to the front buffer becoming unavailable depends on whether WPF is enabled to fall back to software rendering.</span></span> <span data-ttu-id="e2d39-160"><xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 메서드 소프트웨어 렌더링으로 WPF 다시 속하는지 여부를 지정 하는 매개 변수를 받아들이는 오버 로드가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-160">The <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> method has an overload that takes a parameter that specifies whether WPF falls back to software rendering.</span></span>  
  
 <span data-ttu-id="e2d39-161">호출 하는 경우는 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%28System.Windows.Interop.D3DResourceType%2CSystem.IntPtr%29> 오버 로드 하거나 호출 합니다 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%28System.Windows.Interop.D3DResourceType%2CSystem.IntPtr%2CSystem.Boolean%29> 오버 로드는 `enableSoftwareFallback` 매개 변수 설정 `false`, 프런트 버퍼를 사용할 수 없게 하 고 아무 것도 렌더링 시스템 백 버퍼에 대 한 참조를 해제 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-161">When you call the <xref:System.Windows.Interop.D3DImage.SetBackBuffer%28System.Windows.Interop.D3DResourceType%2CSystem.IntPtr%29> overload or call the <xref:System.Windows.Interop.D3DImage.SetBackBuffer%28System.Windows.Interop.D3DResourceType%2CSystem.IntPtr%2CSystem.Boolean%29> overload with the `enableSoftwareFallback` parameter set to `false`, the rendering system releases its reference to the back buffer when the front buffer becomes unavailable and nothing is displayed.</span></span> <span data-ttu-id="e2d39-162">프런트 버퍼가 다시 제공 되 면 렌더링 발생을 <xref:System.Windows.Interop.D3DImage.IsFrontBufferAvailableChanged> WPF 애플리케이션에 알리는 이벤트입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-162">When the front buffer is available again, the rendering system raises the <xref:System.Windows.Interop.D3DImage.IsFrontBufferAvailableChanged> event to notify your WPF application.</span></span>  <span data-ttu-id="e2d39-163">에 대 한 이벤트 처리기를 만들 수는 <xref:System.Windows.Interop.D3DImage.IsFrontBufferAvailableChanged> 유효한 Direct3D 화면을 사용 하 여 다시 렌더링을 다시 시작 하는 이벤트입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-163">You can create an event handler for the <xref:System.Windows.Interop.D3DImage.IsFrontBufferAvailableChanged> event to restart rendering again with a valid Direct3D surface.</span></span> <span data-ttu-id="e2d39-164">렌더링을 다시 시작 하려면 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A>합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-164">To restart rendering, you must call <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A>.</span></span>  
  
 <span data-ttu-id="e2d39-165">호출 하는 경우는 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%28System.Windows.Interop.D3DResourceType%2CSystem.IntPtr%2CSystem.Boolean%29> 오버 로드는 `enableSoftwareFallback` 매개 변수 설정 `true`를 호출 하지 않아도 되므로 프런트 버퍼를 사용할 수 없을 때 렌더링 시스템은 백 버퍼에 대 한 참조를 유지 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 때 앞 버퍼를 다시 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-165">When you call the <xref:System.Windows.Interop.D3DImage.SetBackBuffer%28System.Windows.Interop.D3DResourceType%2CSystem.IntPtr%2CSystem.Boolean%29> overload with the `enableSoftwareFallback` parameter set to `true`, the rendering system retains its reference to the back buffer when the front buffer becomes unavailable, so there is no need to call <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> when the front buffer is available again.</span></span>  
  
 <span data-ttu-id="e2d39-166">소프트웨어 렌더링을 사용 하는 경우 여기서 사용자의 장치를 사용할 수 없지만 렌더링 시스템은 Direct3D 화면에 대 한 참조를 유지 하는 경우 있을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-166">When software rendering is enabled, there may be situations where the user’s device becomes unavailable, but the rendering system retains a reference to the Direct3D surface.</span></span> <span data-ttu-id="e2d39-167">Direct3D9 장치를 사용할 수 있는지를 확인 하려면 호출을 `TestCooperativeLevel` 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-167">To check whether a Direct3D9 device is unavailable, call the `TestCooperativeLevel` method.</span></span> <span data-ttu-id="e2d39-168">Direct3D9Ex 장치 호출을 확인 하는 `CheckDeviceState` 메서드를 때문에 `TestCooperativeLevel` 메서드는 사용 되지 않으며 항상 성공 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-168">To check a Direct3D9Ex devices call the `CheckDeviceState` method, because the `TestCooperativeLevel` method is deprecated and always returns success.</span></span> <span data-ttu-id="e2d39-169">사용자 장치를 사용할 경우 호출 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 백 버퍼에 대 한 WPF의 참조를 해제 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-169">If the user device has become unavailable, call <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> to release WPF’s reference to the back buffer.</span></span>  <span data-ttu-id="e2d39-170">디바이스를 재설정 해야 할 경우 호출 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 사용 하 여는 `backBuffer` 매개 변수 설정 `null`, 다음 호출 <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> 사용 하 여 다시 `backBuffer` 유효한 Direct3D 화면을로 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-170">If you need to reset your device, call <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> with the `backBuffer` parameter set to `null`, and then call <xref:System.Windows.Interop.D3DImage.SetBackBuffer%2A> again with `backBuffer` set to a valid Direct3D surface.</span></span>  
  
 <span data-ttu-id="e2d39-171">호출 된 `Reset` 다중 어댑터 지원을 구현 하는 경우에 잘못 된 장치에서 복구 하는 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-171">Call the `Reset` method to recover from an invalid device only if you implement multi-adapter support.</span></span> <span data-ttu-id="e2d39-172">아니면 모든 Direct3D9 인터페이스를 해제 하 고 전체적으로 다시 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-172">Otherwise, release all Direct3D9 interfaces and re-create them completely.</span></span> <span data-ttu-id="e2d39-173">어댑터 레이아웃 변경 된 경우에 변경 전에 만들어진 Direct3D9 개체 업데이트 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-173">If the adapter layout has changed, Direct3D9 objects created before the change are not updated.</span></span>  
  
## <a name="handling-resizing"></a><span data-ttu-id="e2d39-174">크기 조정을 처리합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-174">Handling Resizing</span></span>  
 <span data-ttu-id="e2d39-175">경우는 <xref:System.Windows.Interop.D3DImage> 표시 됩니다 현재에 따라 확장은 기본 크기로 이외의 해상도 <xref:System.Windows.Media.RenderOptions.BitmapScalingMode%2A>점을 제외 하 고 <xref:System.Windows.Media.Effects.SamplingMode.Bilinear> 대체 <xref:System.Windows.Media.BitmapScalingMode.Fant>합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-175">If a <xref:System.Windows.Interop.D3DImage> is displayed at a resolution other than its native size, it is scaled according to the current <xref:System.Windows.Media.RenderOptions.BitmapScalingMode%2A>, except that <xref:System.Windows.Media.Effects.SamplingMode.Bilinear> is substituted for <xref:System.Windows.Media.BitmapScalingMode.Fant>.</span></span>  
  
 <span data-ttu-id="e2d39-176">충실도 높아집니다, 필요한 경우 새로 만들어야 합니다 노출 하는 경우의 컨테이너는 <xref:System.Windows.Interop.D3DImage> 크기를 변경 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-176">If you require higher fidelity, you must create a new surface when the container of the <xref:System.Windows.Interop.D3DImage> changes size.</span></span>  
  
 <span data-ttu-id="e2d39-177">세 가지 방법으로 크기 조정 처리할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-177">There are three possible approaches to handle resizing.</span></span>  
  
-   <span data-ttu-id="e2d39-178">레이아웃 시스템에 참여 하 고 크기를 변경할 경우 새 화면을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-178">Participate in the layout system and create a new surface when the size changes.</span></span> <span data-ttu-id="e2d39-179">가 소진 되어 수도 비디오 메모리 조각화 때문에 화면을 너무 많이 만들지 마십시오.</span><span class="sxs-lookup"><span data-stu-id="e2d39-179">Do not create too many surfaces, because you may exhaust or fragment video memory.</span></span>  
  
-   <span data-ttu-id="e2d39-180">새 화면을 만들려면 고정된 기간 크기 조정 이벤트를 발생 하지 않은 때까지 기다립니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-180">Wait until a resize event has not occurred for a fixed period of time to create the new surface.</span></span>  
  
-   <span data-ttu-id="e2d39-181">만들기는 <xref:System.Windows.Threading.DispatcherTimer> 컨테이너 크기를 초당 여러 번 확인 하는 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-181">Create a <xref:System.Windows.Threading.DispatcherTimer> that checks the container dimensions several times per second.</span></span>  
  
## <a name="multi-monitor-optimization"></a><span data-ttu-id="e2d39-182">다중 모니터 최적화</span><span class="sxs-lookup"><span data-stu-id="e2d39-182">Multi-monitor Optimization</span></span>  
 <span data-ttu-id="e2d39-183">렌더링 시스템으로 이동 하는 경우 성능이 크게 저하 될 수 있습니다는 <xref:System.Windows.Interop.D3DImage> 다른 모니터로 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-183">Significantly reduced performance can result when the rendering system moves a <xref:System.Windows.Interop.D3DImage> to another monitor.</span></span>  
  
 <span data-ttu-id="e2d39-184">그러나 WDDM에 동일한 비디오 모니터 되는 카드를 사용 하 여 `Direct3DCreate9Ex`, 성능이 저하 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-184">On WDDM, as long as the monitors are on the same video card and you use `Direct3DCreate9Ex`, there is no reduction in performance.</span></span> <span data-ttu-id="e2d39-185">모니터는 별도 비디오 카드에 있는 경우 성능이 저하 됩니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-185">If the monitors are on separate video cards, performance is reduced.</span></span> <span data-ttu-id="e2d39-186">Windows xp에서는 성능이 항상 저하 됩니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-186">On Windows XP, performance is always reduced.</span></span>  
  
 <span data-ttu-id="e2d39-187">경우는 <xref:System.Windows.Interop.D3DImage> 좋은 성능을 복원 하려면 해당 어댑터에 새 화면을 만들 수 있습니다 다른 모니터로 이동 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-187">When the <xref:System.Windows.Interop.D3DImage> moves to another monitor, you can create a new surface on the corresponding adapter to restore good performance.</span></span>  
  
 <span data-ttu-id="e2d39-188">성능 저하를 방지 하려면 다중 모니터에 맞게 코드를 작성 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-188">To avoid the performance penalty, write code specifically for the multi-monitor case.</span></span> <span data-ttu-id="e2d39-189">다음은 다중 모니터 코드를 작성 하는 한 가지 방법은 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-189">The following list shows one way to write multi-monitor code.</span></span>  
  
1.  <span data-ttu-id="e2d39-190">지점을 찾는 합니다 <xref:System.Windows.Interop.D3DImage> 화면 공간에는 `Visual.ProjectToScreen` 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-190">Find a point of the <xref:System.Windows.Interop.D3DImage> in screen space with the `Visual.ProjectToScreen` method.</span></span>  
  
2.  <span data-ttu-id="e2d39-191">사용 된 `MonitorFromPoint` GDI 메서드 시점을 표시 하는 모니터를 찾습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-191">Use the `MonitorFromPoint` GDI method to find the monitor that is displaying the point.</span></span>  
  
3.  <span data-ttu-id="e2d39-192">사용 된 `IDirect3D9::GetAdapterMonitor` 모니터 Direct3D9 어댑터를 찾으려면 메서드는 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-192">Use the `IDirect3D9::GetAdapterMonitor` method to find which Direct3D9 adapter the monitor is on.</span></span>  
  
4.  <span data-ttu-id="e2d39-193">어댑터 백 버퍼를 사용 하 여 어댑터와 동일 하 게 없는 경우 새 백 버퍼에 새 모니터 만들고 할당 하 여 <xref:System.Windows.Interop.D3DImage> 백 버퍼입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-193">If the adapter is not the same as the adapter with the back buffer, create a new back buffer on the new monitor and assign it to the <xref:System.Windows.Interop.D3DImage> back buffer.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="e2d39-194">경우는 <xref:System.Windows.Interop.D3DImage> 여러 모니터 성능 WDDM의 경우 제외 하 고 느려질 수 및 `IDirect3D9Ex` 동일한 어댑터에 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-194">If the <xref:System.Windows.Interop.D3DImage> straddles monitors, performance will be slow, except in the case of WDDM and `IDirect3D9Ex` on the same adapter.</span></span> <span data-ttu-id="e2d39-195">이 상황에서 성능을 향상 시키는 방법이 없습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-195">There is no way to improve performance in this situation.</span></span>  
  
 <span data-ttu-id="e2d39-196">다음 코드 예제에는 현재 모니터를 찾는 방법을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-196">The following code example shows how to find the current monitor.</span></span>  
  
 [!code-cpp[System.Windows.Interop.D3DImage#RendererManager_SetAdapter](../../../../samples/snippets/cpp/VS_Snippets_Wpf/System.Windows.Interop.D3DImage/cpp/renderermanager.cpp#renderermanager_setadapter)]  
  
 <span data-ttu-id="e2d39-197">모니터를 업데이트 때 합니다 <xref:System.Windows.Interop.D3DImage> 컨테이너의 크기 또는 위치 변경 또는 업데이트를 사용 하 여 모니터를 `DispatcherTimer` 초당 여러 번 업데이트 하는 합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-197">Update the monitor when the <xref:System.Windows.Interop.D3DImage> container's size or position changes, or update the monitor by using a `DispatcherTimer` that updates a few times per second.</span></span>  
  
## <a name="wpf-software-rendering"></a><span data-ttu-id="e2d39-198">WPF 소프트웨어 렌더링</span><span class="sxs-lookup"><span data-stu-id="e2d39-198">WPF Software Rendering</span></span>  
 <span data-ttu-id="e2d39-199">WPF는 다음과 같은 경우에는 소프트웨어에서 UI 스레드에서 동기적으로 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-199">WPF renders synchronously on the UI thread in software in the following situations.</span></span>  
  
-   <span data-ttu-id="e2d39-200">인쇄</span><span class="sxs-lookup"><span data-stu-id="e2d39-200">Printing</span></span>  
  
-   <xref:System.Windows.Media.Effects.BitmapEffect>  
  
-   <xref:System.Windows.Media.Imaging.RenderTargetBitmap>  
  
 <span data-ttu-id="e2d39-201">이러한 상황 중 하나가 발생 하면 렌더링 시스템 호출을 <xref:System.Windows.Interop.D3DImage.CopyBackBuffer%2A> 소프트웨어 하드웨어 버퍼를 복사 하는 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-201">When one of these situations occurs, the rendering system calls the <xref:System.Windows.Interop.D3DImage.CopyBackBuffer%2A> method to copy the hardware buffer to software.</span></span> <span data-ttu-id="e2d39-202">기본 구현 호출을 `GetRenderTargetData` 에 화면을 사용 하 여 메서드.</span><span class="sxs-lookup"><span data-stu-id="e2d39-202">The default implementation calls the `GetRenderTargetData` method with your surface.</span></span> <span data-ttu-id="e2d39-203">이 호출은 잠금/잠금 해제 패턴 외부에서 발생 하기 때문에 실패할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-203">Because this call occurs outside of the Lock/Unlock pattern, it may fail.</span></span> <span data-ttu-id="e2d39-204">이 경우에 `CopyBackBuffer` 메서드가 반환 되는 `null` 및 이미지가 표시 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-204">In this case, the `CopyBackBuffer` method returns `null` and no image is displayed.</span></span>  
  
 <span data-ttu-id="e2d39-205">재정의할 수 있습니다 합니다 <xref:System.Windows.Interop.D3DImage.CopyBackBuffer%2A> 메서드를 기본 구현을 반환 하는 경우 및 `null`, 자리 표시자를 반환할 수 있습니다 <xref:System.Windows.Media.Imaging.BitmapSource>합니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-205">You can override the <xref:System.Windows.Interop.D3DImage.CopyBackBuffer%2A> method, call the base implementation, and if it returns `null`, you can return a placeholder <xref:System.Windows.Media.Imaging.BitmapSource>.</span></span>  
  
 <span data-ttu-id="e2d39-206">또한 기본 구현을 호출 하는 대신 고유한 소프트웨어 렌더링을 구현할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-206">You can also implement your own software rendering instead of calling the base implementation.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="e2d39-207">WPF가 완전히 소프트웨어에서 렌더링 하는 경우 <xref:System.Windows.Interop.D3DImage> WPF 프런트 버퍼가 없기 때문에 표시 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e2d39-207">If WPF is rendering completely in software, <xref:System.Windows.Interop.D3DImage> is not shown because WPF does not have a front buffer.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e2d39-208">참고자료</span><span class="sxs-lookup"><span data-stu-id="e2d39-208">See also</span></span>
- <xref:System.Windows.Interop.D3DImage>
- [<span data-ttu-id="e2d39-209">Direct3D9 및 WPF 상호 운용성을 위한 성능 고려 사항</span><span class="sxs-lookup"><span data-stu-id="e2d39-209">Performance Considerations for Direct3D9 and WPF Interoperability</span></span>](../../../../docs/framework/wpf/advanced/performance-considerations-for-direct3d9-and-wpf-interoperability.md)
- [<span data-ttu-id="e2d39-210">연습: WPF에서 호스팅할 Direct3D9 콘텐츠 만들기</span><span class="sxs-lookup"><span data-stu-id="e2d39-210">Walkthrough: Creating Direct3D9 Content for Hosting in WPF</span></span>](../../../../docs/framework/wpf/advanced/walkthrough-creating-direct3d9-content-for-hosting-in-wpf.md)
- [<span data-ttu-id="e2d39-211">연습: WPF에서 Direct3D9 콘텐츠 호스팅</span><span class="sxs-lookup"><span data-stu-id="e2d39-211">Walkthrough: Hosting Direct3D9 Content in WPF</span></span>](../../../../docs/framework/wpf/advanced/walkthrough-hosting-direct3d9-content-in-wpf.md)
