---
title: Docker 애플리케이션의 상태 및 데이터
description: 컨테이너 화 된 응용 프로그램의 상태를 저장 하는 사용 가능한 옵션에 알아봅니다.
author: CESARDELATORRE
ms.author: wiwagn
ms.date: 02/15/2019
ms.openlocfilehash: 1e30a545ba0003acb8b85dee9896d54934f0d737
ms.sourcegitcommit: 8f95d3a37e591963ebbb9af6e90686fd5f3b8707
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 02/23/2019
ms.locfileid: "56746000"
---
# <a name="state-and-data-in-docker-applications"></a><span data-ttu-id="0662c-103">Docker 애플리케이션의 상태 및 데이터</span><span class="sxs-lookup"><span data-stu-id="0662c-103">State and data in Docker applications</span></span>

<span data-ttu-id="0662c-104">대부분의 경우에 컨테이너를 프로세스의 인스턴스로 간주할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-104">In most cases, you can think of a container as an instance of a process.</span></span> <span data-ttu-id="0662c-105">프로세스는 영구 상태를 유지하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-105">A process does not maintain persistent state.</span></span> <span data-ttu-id="0662c-106">를 로컬 저장소 컨테이너를 작성할 수 있습니다 메모리의 단일 위치 영 속 된다고 가정 하 고 비슷합니다 인스턴스 되도록 주위 무기한으로 가정 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-106">While a container can write to its local storage, assuming that an instance will be around indefinitely is like assuming that a single location in memory will be durable.</span></span> <span data-ttu-id="0662c-107">컨테이너 이미지를 프로세스와 같은 여러 인스턴스 가정 및는 결국 종료 합니다. 경우는 컨테이너 오 케 스트레이 터를 사용 하 여 관리 되는 경우를 가정 하는 이동 될 수 노드 또는 VM에서 다른 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-107">Container images, like processes, should be assumed to have multiple instances and that they will eventually be killed; if they're managed with a container orchestrator, it should be assumed that they might get moved from one node or VM to another.</span></span>

<span data-ttu-id="0662c-108">다음 솔루션은 Docker 애플리케이션에서 영구 데이터를 관리하는 데 사용됩니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-108">The following solutions are used to manage persistent data in Docker applications:</span></span>

<span data-ttu-id="0662c-109">Docker 호스트에서 [Docker 볼륨](https://docs.docker.com/engine/admin/volumes/)으로:</span><span class="sxs-lookup"><span data-stu-id="0662c-109">From the Docker host, as [Docker Volumes](https://docs.docker.com/engine/admin/volumes/):</span></span>

- <span data-ttu-id="0662c-110">**볼륨** Docker에서 관리 되는 호스트 파일 시스템의 영역에 저장 됩니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-110">**Volumes** are stored in an area of the host filesystem that is managed by Docker.</span></span>

- <span data-ttu-id="0662c-111">**탑재 바인딩** 액세스는 Docker 프로세스에서 제어할 수 없습니다 및 컨테이너를 중요 한 OS 폴더에 액세스할 수 없습니다 보안상 위험할 수 있으므로 호스트 파일 시스템의 폴더를 매핑할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-111">**Bind mounts** can map to any folder in the host filesystem, so access can't be controlled from a Docker process and can pose a security risk as a container could access sensitive OS folders.</span></span>

- <span data-ttu-id="0662c-112">**tmpfs 탑재**는 호스트 메모리에만 존재하고 파일 시스템에 기록되지 않는 가상 폴더와 같습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-112">**tmpfs mounts** are like virtual folders that only exist in the host's memory and are never written to the filesystem.</span></span>

<span data-ttu-id="0662c-113">원격 스토리지에서:</span><span class="sxs-lookup"><span data-stu-id="0662c-113">From remote storage:</span></span>

- <span data-ttu-id="0662c-114">[Azure Storage](https://azure.microsoft.com/documentation/services/storage/) 컨테이너에 대 한 적합 한 장기 지 속성 솔루션을 제공 하는 지역 배포 가능한 저장소를 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-114">[Azure Storage](https://azure.microsoft.com/documentation/services/storage/) provides geo-distributable storage, providing a good long-term persistence solution for containers.</span></span>

- <span data-ttu-id="0662c-115">같은 원격 관계형 데이터베이스 [Azure SQL Database](https://azure.microsoft.com/services/sql-database/), 같은 NoSQL 데이터베이스 [Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), 또는 같은 서비스 캐시 [Redis](https://redis.io/)합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-115">Remote relational databases like [Azure SQL Database](https://azure.microsoft.com/services/sql-database/), NoSQL databases like [Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), or cache services like [Redis](https://redis.io/).</span></span>

<span data-ttu-id="0662c-116">Docker 컨테이너에서:</span><span class="sxs-lookup"><span data-stu-id="0662c-116">From the Docker container:</span></span>

- <span data-ttu-id="0662c-117">Docker는 *오버레이 파일 시스템*이라는 기능을 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-117">Docker provides a feature named the *overlay file system*.</span></span> <span data-ttu-id="0662c-118">이 기능은 저장소 컨테이너의 루트 파일 시스템에 정보를 업데이트 하는 쓰기 시 복사 작업을 구현 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-118">This feature implements a copy-on-write task that stores updated information to the root file system of the container.</span></span> <span data-ttu-id="0662c-119">해당 정보 "레이아웃의 상단에서" 컨테이너의 기반이 되는 원본 이미지입니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-119">That information "lays on top of" the original image on which the container is based.</span></span> <span data-ttu-id="0662c-120">시스템에서 컨테이너를 삭제하는 경우 해당 변경 내용이 손실됩니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-120">If the container is deleted from the system, those changes are lost.</span></span> <span data-ttu-id="0662c-121">따라서 로컬 저장소 내 컨테이너의 상태를 저장할 수 있지만, 기본적으로 상태 비저장 인 컨테이너 디자인의 전제와 충돌이 기능을 기반으로 시스템을 디자인 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-121">Therefore, while it is possible to save the state of a container within its local storage, designing a system based on this feature would conflict with the premise of container design, which by default is stateless.</span></span>

- <span data-ttu-id="0662c-122">그러나 Docker 볼륨 Docker에서 로컬 데이터를 처리 하는 기본 방법은 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-122">However, Docker Volumes is now the preferred way to handle local data in Docker.</span></span> <span data-ttu-id="0662c-123">저장소 컨테이너에 대 한 자세한 내용은, 필요한 경우 확인할 [Docker 저장소 드라이버](https://docs.docker.com/engine/userguide/storagedriver/) 하 고 [이미지, 컨테이너 및 저장소 드라이버에 대 한](https://docs.docker.com/engine/userguide/storagedriver/imagesandcontainers/)합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-123">If you need more information about storage in containers, check on [Docker storage drivers](https://docs.docker.com/engine/userguide/storagedriver/) and [About images, containers, and storage drivers](https://docs.docker.com/engine/userguide/storagedriver/imagesandcontainers/).</span></span>

<span data-ttu-id="0662c-124">다음 이러한 옵션에 대 한 추가 정보를 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-124">The following provides additional detail about these options.</span></span>

<span data-ttu-id="0662c-125">**볼륨**은 컨테이너에 있는 호스트 OS에서 디렉터리에 매핑된 디렉터리입니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-125">**Volumes** are directories mapped from the host OS to directories in containers.</span></span> <span data-ttu-id="0662c-126">컨테이너의 코드에 디렉터리에 대한 액세스 권한이 있는 경우 실제로 호스트 OS의 디렉터리에 액세스할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-126">When code in the container has access to the directory, that access is actually to a directory on the host OS.</span></span> <span data-ttu-id="0662c-127">이 디렉터리는 컨테이너 자체의 수명과 관련이 없으며, 디렉터리는 Docker에 의해 관리되고 호스트 머신의 핵심 기능과 격리되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-127">This directory is not tied to the lifetime of the container itself, and the directory is managed by Docker and isolated from the core functionality of the host machine.</span></span> <span data-ttu-id="0662c-128">따라서 데이터 볼륨은 컨테이너의 수명과 독립적으로 데이터를 유지하도록 설계되었습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-128">Thus, data volumes are designed to persist data independently of the life of the container.</span></span> <span data-ttu-id="0662c-129">Docker 호스트에서 컨테이너 또는 이미지를 삭제하는 경우 데이터 볼륨에서 유지되는 데이터는 삭제되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-129">If you delete a container or an image from the Docker host, the data persisted in the data volume is not deleted.</span></span>

<span data-ttu-id="0662c-130">볼륨은 이름을 지정하거나 익명화할 수 있습니다(기본값).</span><span class="sxs-lookup"><span data-stu-id="0662c-130">Volumes can be named or anonymous (the default).</span></span> <span data-ttu-id="0662c-131">명명된 볼륨은 **데이터 볼륨 컨테이너**의 발전이며 컨테이너 간에 데이터를 쉽게 공유할 수 있도록 해줍니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-131">Named volumes are the evolution of **Data Volume Containers** and make it easy to share data between containers.</span></span> <span data-ttu-id="0662c-132">볼륨도 다른 옵션과 함께 원격 호스트에서 데이터를 저장할 수 있도록 하는 볼륨 드라이버를 지원 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-132">Volumes also support volume drivers that allow you to store data on remote hosts, among other options.</span></span>

<span data-ttu-id="0662c-133">**탑재 바인딩** 오랜 시간 동안 제공 되었습니다 및 컨테이너에 탑재 지점 폴더의 매핑을 허용 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-133">**Bind mounts** have been available for a long time and allow the mapping of any folder to a mount point in a container.</span></span> <span data-ttu-id="0662c-134">탑재 바인딩은 볼륨보다 더 많은 제한 사항과 몇 가지 중요한 보안 문제가 있으므로 볼륨이 권장되는 옵션입니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-134">Bind mounts have more limitations than volumes and some important security issues, so volumes are the recommended option.</span></span>

<span data-ttu-id="0662c-135">**`tmpfs` 탑재** 는 가상 폴더는 호스트의 메모리에만 라이브 및 파일 시스템에 기록 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-135">**`tmpfs` mounts** are virtual folders that live only in the host's memory and are never written to the filesystem.</span></span> <span data-ttu-id="0662c-136">이들은 빠르고 안전하지만 메모리를 사용하며 비영구적 데이터에만 사용됩니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-136">They are fast and secure but use memory and are only meant for non-persistent data.</span></span>

<span data-ttu-id="0662c-137">그림 4-5와 같이 일반 Docker 볼륨은 호스트 서버 또는 VM의 물리적 장벽 내부가 아닌 컨테이너 외부에 저장될 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-137">As shown in Figure 4-5, regular Docker volumes can be stored outside of the containers themselves but within the physical boundaries of the host server or VM.</span></span> <span data-ttu-id="0662c-138">그러나 Docker 컨테이너는 호스트 서버 또는 VM 간의 볼륨에 액세스할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-138">However, Docker containers cannot access a volume from one host server or VM to another.</span></span> <span data-ttu-id="0662c-139">즉, 이러한 볼륨 이므로 없이 원격 호스트를 지 원하는 볼륨 드라이버를 사용 하 여 설정할 수 있지만 여러 Docker 호스트에서 실행 되는 컨테이너 간에 공유 된 데이터를 관리할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-139">In other words, with these volumes, it is not possible to manage data shared between containers that run on different Docker hosts, although it could be achieved with a volume driver that supports remote hosts.</span></span>

![<span data-ttu-id="0662c-140">볼륨을 컨테이너 간에 공유할 수 있지만 원격 호스트를 지원하는 원격 드라이버를 사용하지 않는 한 동일한 호스트에서만 공유할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-140">Volumes can be shared between containers, but only in the same host, unless you use a remote driver that supports remote hosts.</span></span> ](./media/image5.png)

<span data-ttu-id="0662c-141">**그림 4-5**</span><span class="sxs-lookup"><span data-stu-id="0662c-141">**Figure 4-5**.</span></span> <span data-ttu-id="0662c-142">컨테이너 기반 애플리케이션에 대한 볼륨 및 외부 데이터 원본</span><span class="sxs-lookup"><span data-stu-id="0662c-142">Volumes and external data sources for container-based applications</span></span>

<span data-ttu-id="0662c-143">또한 오케스트레이터에서 Docker 컨테이너를 관리할 때 컨테이너는 클러스터에서 수행한 최적화에 따라 호스트 간에 "이동"할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-143">In addition, when Docker containers are managed by an orchestrator, containers might "move" between hosts, depending on the optimizations performed by the cluster.</span></span> <span data-ttu-id="0662c-144">따라서 비즈니스 데이터에 데이터 볼륨을 사용하는 것을 권장하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-144">Therefore, it is not recommended that you use data volumes for business data.</span></span> <span data-ttu-id="0662c-145">하지만 추적 파일, 임시 파일을 사용 하 여 작업 하는 적절 한 메커니즘이 되거나 마찬가지로 영향을 주지 않는 비즈니스 데이터 일관성.</span><span class="sxs-lookup"><span data-stu-id="0662c-145">But they are a good mechanism to work with trace files, temporal files, or similar, that will not impact business data consistency.</span></span>

<span data-ttu-id="0662c-146">컨테이너 없이 개발할 때 사용하는 것과 동일한 방식으로 Azure SQL Database와 같은 **원격 데이터 원본 및 캐시** 도구, Azure Cosmos DB 또는 Redis와 같은 원격 캐시를 컨테이너화된 애플리케이션에서 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-146">**Remote data sources and cache** tools like Azure SQL Database, Azure Cosmos DB, or a remote cache like Redis can be used in containerized applications the same way they are used when developing without containers.</span></span> <span data-ttu-id="0662c-147">비즈니스 애플리케이션 데이터를 저장하는 검증된 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-147">This is a proven way to store business application data.</span></span>

<span data-ttu-id="0662c-148">**Azure Storage.**</span><span class="sxs-lookup"><span data-stu-id="0662c-148">**Azure Storage.**</span></span> <span data-ttu-id="0662c-149">비즈니스 데이터는 일반적으로 외부 리소스 또는 Azure Storage와 같은 데이터베이스에 배치 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-149">Business data usually needs to be placed in external resources or databases, like Azure Storage.</span></span> <span data-ttu-id="0662c-150">Azure Storage는 클라우드의 다음 서비스를 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-150">Azure Storage provides the following services in the cloud:</span></span>

- <span data-ttu-id="0662c-151">Blob 스토리지는 구조화되지 않은 개체 데이터를 저장합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-151">Blob storage stores unstructured object data.</span></span> <span data-ttu-id="0662c-152">Blob는 문서 또는 미디어 파일(이미지, 오디오 및 비디오 파일)과 같은 텍스트 또는 이진 데이터 형식일 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-152">A blob can be any type of text or binary data, such as document or media files (images, audio, and video files).</span></span> <span data-ttu-id="0662c-153">Blob 스토리지를 개체 스토리지라고도 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-153">Blob storage is also referred to as Object storage.</span></span>

- <span data-ttu-id="0662c-154">File storage는 표준 SMB 프로토콜을 사용 하 여 레거시 응용 프로그램을 위한 공유 저장소를 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-154">File storage offers shared storage for legacy applications using the standard SMB protocol.</span></span> <span data-ttu-id="0662c-155">Azure 가상 머신 및 클라우드 서비스는 탑재된 공유를 통해 애플리케이션 구성 요소 간에 파일 데이터를 공유할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-155">Azure virtual machines and cloud services can share file data across application components via mounted shares.</span></span> <span data-ttu-id="0662c-156">온-프레미스 응용 프로그램에는 파일 공유의 데이터에는 파일 서비스 REST API를 통해 액세스할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-156">On-premises applications can access file data in a share via the File Service REST API.</span></span>

- <span data-ttu-id="0662c-157">테이블 스토리지는 구조화된 데이터 세트를 저장합니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-157">Table storage stores structured datasets.</span></span> <span data-ttu-id="0662c-158">테이블 스토리지는 NoSQL 키 특성 데이터 저장소입니다. 이를 통해 많은 양의 데이터를 신속하게 개발하고 빠르게 액세스할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-158">Table storage is a NoSQL key-attribute data store, which allows rapid development and fast access to large quantities of data.</span></span>

<span data-ttu-id="0662c-159">**관계형 데이터베이스 및 NoSQL 데이터베이스**</span><span class="sxs-lookup"><span data-stu-id="0662c-159">**Relational databases and NoSQL databases.**</span></span> <span data-ttu-id="0662c-160">SQL Server, PostgreSQL, Oracle과 같은 관계형 데이터베이스 또는 Azure Cosmos DB, MongoDB와 같은 NoSQL 데이터베이스 등 다양한 외부 데이터베이스가 있습니다. 이러한 데이터베이스는 완전히 다른 항목을 사용 되기 때문에이 가이드의 일부로 설명 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="0662c-160">There are many choices for external databases, from relational databases like SQL Server, PostgreSQL, Oracle, or NoSQL databases like Azure Cosmos DB, MongoDB, etc. These databases are not going to be explained as part of this guide since they are a different topic altogether.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="0662c-161">[이전](monolithic-applications.md)
>[다음](soa-applications.md)</span><span class="sxs-lookup"><span data-stu-id="0662c-161">[Previous](monolithic-applications.md)
[Next](soa-applications.md)</span></span>
